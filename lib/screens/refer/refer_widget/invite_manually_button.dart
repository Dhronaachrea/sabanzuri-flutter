part of 'refer_widget.dart';

class InviteManuallyButton extends StatelessWidget {
  final bool isDarkThemeOn;
  final VoidCallback onAddMore;
  final VoidCallback onInviteNow;

  const InviteManuallyButton({
    Key? key,
    required this.isDarkThemeOn,
    required this.onAddMore,
    required this.onInviteNow,
  }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
